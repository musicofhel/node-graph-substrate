[project]
name = "topoconf-daemon"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "redis>=5.2",
    "numpy>=1.26",
    "scikit-learn>=1.5",
    "torch>=2.0",
    "transformers>=4.40",
]

[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"
